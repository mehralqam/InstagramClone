    <p>im in story index</p>

<div  class=" img-fluid img-thumbnail rounded mx-auto d-block w-50  border bg-light">
   <% @stories.each do |story|%>
      <%# <%= link_to "#{story.id}", post_path(story.id) %>
       <%=  image_tag story.image, width: "500px"%>
   <%end%>

   <%= paginate @stories %>
</div>

