<% if @user.present? %>
  <%= render partial: 'dashboard' ,locals: { user: @user}%>
  <% if current_user.followers.include?(@user) %>
    <%= render partial: 'posts/all_posts' ,locals: { posts: @user.posts } %>
  <%end%>
<% end %>
